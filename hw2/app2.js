const firstValue = prompt('Enter the first value:', 0);

const secondValue = prompt('Enter the second value:', 0);

const comparisonOfNumber = firstValue === secondValue;

const alertResult = `Your result:
${firstValue} = ${secondValue} is ${comparisonOfNumber};`;

alert(alertResult);
